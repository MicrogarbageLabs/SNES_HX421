diff --git a/src/Makefile b/src/Makefile
index 034cd96..97e951f 100644
--- a/src/Makefile
+++ b/src/Makefile
@@ -83,6 +83,7 @@ SRC += cli.c ymodem.c rle.c msu1.c crc16.c sysinfo.c
 SRC += cfg.c cheat.c yaml.c savestate.c sgb.c hwinfo.c
 
 SRC += usbdesc.c usbcore.c usbuser.c cdcuser.c usbinterface.c
+SRC += hx421_loader.c hx421_sysimpl.c hx421_fw.c hx421_termcdc.c
 
 # List Assembler source files here.
 #     Make them always end in a capital .S.  Files ending in a lowercase .s
@@ -124,6 +125,10 @@ CSTANDARD = -std=gnu99
 
 # Place -D or -U options here
 CDEFS = -DF_OSC=$(CONFIG_MCU_FOSC)UL
+# HX-421 dev-mode: route the single USB CDC port to the interactive text CLI
+# (ls/run/game output) instead of the binary usb2snes protocol. Descriptor
+# unchanged; only the CDC data path is rewired. See docs/dev-mode.md.
+CDEFS += -DHX421_DEV_TERMINAL
 
 # Place -I options here
 CINCS = -Iinclude
diff --git a/src/cdcuser.c b/src/cdcuser.c
index 2b42bd8..ade78a2 100644
--- a/src/cdcuser.c
+++ b/src/cdcuser.c
@@ -32,6 +32,9 @@
 
 #include "usbinterface.h"
 #include CONFIG_MCU_H //interrupt disable
+#ifdef HX421_DEV_TERMINAL
+#include "hx421_termcdc.h"      /* dev-mode: CDC carries the text CLI, not usb2snes */
+#endif
 
 unsigned char BulkBufIn  [USB_CDC_BUFINSIZE];            // Buffer to store USB IN  packet
 unsigned char BulkBufOut [USB_CDC_BUFOUTSIZE];            // Buffer to store USB OUT packet
@@ -341,6 +344,13 @@ int CDC_BulkIn_occupied(void) {
 extern volatile enum usbint_server_state_e server_state;
 void CDC_BulkIn(void) {
   int numBytesSend;
+
+#ifdef HX421_DEV_TERMINAL
+  /* dev-mode: EP2-IN completed — let the console send its next chunk */
+  term_on_in_complete();
+  return;
+#endif
+
   //    printf("called CDC_BulkIn\n");                                                     // split into packets
 
   // *interrupt
@@ -415,6 +425,13 @@ void CDC_BulkIn(void) {
 void CDC_BulkOut(void) {
   int numBytesRead;
 
+#ifdef HX421_DEV_TERMINAL
+  /* dev-mode: hand received bytes to the text console instead of usb2snes */
+  numBytesRead = USB_ReadEP(CDC_DEP_OUT, &BulkBufOut[0]);
+  term_rx_push(BulkBufOut, (uint32_t)numBytesRead);
+  return;
+#endif
+
   // get data from USB into intermediate buffer
   if ( /*!cdc_bulkIN_occupied &&*/ !usbint_server_busy()) {
     numBytesRead = USB_ReadEP(CDC_DEP_OUT, &BulkBufOut[0]);
diff --git a/src/cli.c b/src/cli.c
index 558c80a..48e8fb8 100644
--- a/src/cli.c
+++ b/src/cli.c
@@ -46,6 +46,7 @@
 #include "cic.h"
 #include "ymodem.h"
 #include "rtc.h"
+#include "hx421_fw.h"      /* HX-421 dev-mode: load-and-run a RAM game */
 
 #include "cli.h"
 
@@ -57,8 +58,8 @@ static char *curchar;
 
 /* Word lists */
 static char command_words[] =
-  "cd\0reset\0sreset\0dir\0ls\0test\0exit\0loadrom\0loadraw\0saveraw\0put\0rm\0mkdir\0d4\0vmode\0mapper\0settime\0time\0setfeature\0hexdump\0w8\0w16\0memset\0cheat\0fpgaconf\0chipfeat\0bsregs\0gameloop\0dacboost\0cat\0mv\0";
-enum { CMD_CD = 0, CMD_RESET, CMD_SRESET, CMD_DIR, CMD_LS, CMD_TEST, CMD_EXIT, CMD_LOADROM, CMD_LOADRAW, CMD_SAVERAW, CMD_PUT, CMD_RM, CMD_MKDIR, CMD_D4, CMD_VMODE, CMD_MAPPER, CMD_SETTIME, CMD_TIME, CMD_SETFEATURE, CMD_HEXDUMP, CMD_W8, CMD_W16, CMD_MEMSET, CMD_CHEAT, CMD_FPGACONF, CMD_CHIPFEAT, CMD_BSREGS, CMD_GAMELOOP, CMD_DACBOOST, CMD_CAT, CMD_MV };
+  "cd\0reset\0sreset\0dir\0ls\0test\0exit\0loadrom\0loadraw\0saveraw\0put\0rm\0mkdir\0d4\0vmode\0mapper\0settime\0time\0setfeature\0hexdump\0w8\0w16\0memset\0cheat\0fpgaconf\0chipfeat\0bsregs\0gameloop\0dacboost\0cat\0mv\0run\0";
+enum { CMD_CD = 0, CMD_RESET, CMD_SRESET, CMD_DIR, CMD_LS, CMD_TEST, CMD_EXIT, CMD_LOADROM, CMD_LOADRAW, CMD_SAVERAW, CMD_PUT, CMD_RM, CMD_MKDIR, CMD_D4, CMD_VMODE, CMD_MAPPER, CMD_SETTIME, CMD_TIME, CMD_SETFEATURE, CMD_HEXDUMP, CMD_W8, CMD_W16, CMD_MEMSET, CMD_CHEAT, CMD_FPGACONF, CMD_CHIPFEAT, CMD_BSREGS, CMD_GAMELOOP, CMD_DACBOOST, CMD_CAT, CMD_MV, CMD_RUN };
 
 /* ------------------------------------------------------------------------- */
 /*   Parse functions                                                         */
@@ -368,6 +369,24 @@ void cmd_put(void) {
   }
 }
 
+/* HX-421 dev-mode: load a packed .hxg from SD into the game region and run it.
+ * The game's own file syscalls are confined to a fixed dev sandbox. Blocks until
+ * the game returns (USB/CDC is not serviced during the run yet -- fw_yield is a
+ * stub -- so use games that return for now). */
+void cmd_run(void) {
+  if(*curchar == 0) {
+    printf("Usage: run <file.hxg>\n");
+    return;
+  }
+  printf("loading %s ...\n", curchar);
+  int r = hx421_fw_run_game(curchar, "/sd2snes/hx421");
+  if(r == 0) {
+    printf("game returned OK\n");
+  } else {
+    printf("load/run failed: %d\n", r);
+  }
+}
+
 void cmd_rm(void) {
   FRESULT res = f_unlink(curchar);
   if(res) print_fresult(res, "Error %d removing %s", res, curchar);
@@ -558,10 +577,28 @@ void cli_init(void) {
 }
 
 void cli_entrycheck() {
+#ifdef HX421_DEV_TERMINAL
+  /* dev terminal over USB CDC: enter the CLI on ANY key (not just ESC), and
+   * print a banner so the keypress produces visible output — proving the USB
+   * terminal TX path end to end. */
+  if(uart_gotc()) {
+    uart_getc();                 /* consume the keystroke */
+    printf("\r\n*** HX-421 dev terminal CLI ready (type ? for help) ***\r\n");
+    cli_loop();
+    return;
+  }
+  /* proof-of-life heartbeat: printed periodically to the USB terminal so that
+   * simply opening PuTTY shows the dev firmware is alive and TX works, with no
+   * keypress or CLI entry needed. This poll runs ~50 Hz in menu_main_loop. */
+  static uint32_t hb = 0;
+  if((++hb % 100u) == 0u)
+    printf("[HX-421 dev terminal alive - press any key for CLI]\r\n");
+#else
   if(uart_gotc() && uart_getc() == 27) {
     printf("*** BREAK\n");
     cli_loop();
   }
+#endif
 }
 
 void cli_loop(void) {
@@ -704,6 +741,10 @@ void cli_loop(void) {
       case CMD_MV:
         cmd_mv();
         break;
+
+      case CMD_RUN:
+        cmd_run();
+        break;
     }
   }
 }
diff --git a/src/stm32f401.ld b/src/stm32f401.ld
index 30ef99b..412314f 100644
--- a/src/stm32f401.ld
+++ b/src/stm32f401.ld
@@ -13,7 +13,7 @@ MEMORY
 {
   fwhdr  (r)   : ORIGIN = 0x0800c000, LENGTH = 0x00200 /* space for firmware header */
   flash  (rx)  : ORIGIN = 0x0800c200, LENGTH = 0x33e00 /* leave room for bootldr + metadata */
-  ram    (rwx) : ORIGIN = 0x20000070, LENGTH = 0x0ff90 /* leave room for serial number etc */
+  ram    (rwx) : ORIGIN = 0x20000070, LENGTH = 0x07f90 /* HX-421 dev-mode: cap at 0x20008000 so the top 32K (0x20008000-0x2000FFFF) is the game region; __stack + heap ceiling both land at 0x20008000, static data ends ~0x20004E44 */
 }
 
 SECTIONS
diff --git a/src/stm32f4xx/uart.c b/src/stm32f4xx/uart.c
index 7e042d3..fdbfeda 100644
--- a/src/stm32f4xx/uart.c
+++ b/src/stm32f4xx/uart.c
@@ -8,6 +8,9 @@
 #include "bits.h"
 #include "uart.h"
 #include "led.h"
+#ifdef HX421_DEV_TERMINAL
+#include "hx421_termcdc.h"   /* dev-mode: the console lives on the USB CDC port */
+#endif
 
 static volatile unsigned int read_idx,write_idx;
 
@@ -16,18 +19,35 @@ void uart_putc(char c) {
     uart_putc('\r');
   while(!(BITBAND(UART_REGS->SR, USART_SR_TXE_Pos)));
   UART_REGS->DR = c;
+#ifdef HX421_DEV_TERMINAL
+  term_putc(c);              /* tee to the USB terminal (physical UART still driven) */
+#endif
 }
 
 /* Polling version only */
 unsigned char uart_getc(void) {
+#ifdef HX421_DEV_TERMINAL
+  /* take whichever source has a byte first: USB terminal or physical UART */
+  for (;;) {
+    if (term_avail())
+      return (unsigned char)term_getc();
+    if (BITBAND(UART_REGS->SR, USART_SR_RXNE_Pos))
+      return UART_REGS->DR;
+  }
+#else
   /* wait for character */
   while (!(BITBAND(UART_REGS->SR, USART_SR_RXNE_Pos))) ;
   return UART_REGS->DR;
+#endif
 }
 
 /* Returns true if a char is ready */
 unsigned char uart_gotc(void) {
+#ifdef HX421_DEV_TERMINAL
+  return (BITBAND(UART_REGS->SR, USART_SR_RXNE_Pos) || term_avail());
+#else
   return BITBAND(UART_REGS->SR, USART_SR_RXNE_Pos);
+#endif
 }
 
 void uart_init(void) {
diff --git a/src/usbdesc.c b/src/usbdesc.c
index 6f203e4..070eab9 100644
--- a/src/usbdesc.c
+++ b/src/usbdesc.c
@@ -167,6 +167,20 @@ const uint8_t USB_StringDescriptor[] = {
   '1',0,
 /* Index 0x02: Product */
 /* TODO: Identify as FXPAK Pro from Mk3 Rev.B onwards */
+#ifdef HX421_DEV_TERMINAL
+  /* dev build: a distinct product name so Device Manager confirms at a glance
+   * whether the dev firmware actually flashed (independent of the terminal). */
+  (8*2 + 2),                         /* bLength (8 Char + Type + length) */
+  USB_STRING_DESCRIPTOR_TYPE,        /* bDescriptorType */
+  'H',0,
+  'X',0,
+  '4',0,
+  '2',0,
+  '1',0,
+  'D',0,
+  'E',0,
+  'V',0,
+#else
   (7*2 + 2),                         /* bLength (7 Char + Type + lenght) */
   USB_STRING_DESCRIPTOR_TYPE,        /* bDescriptorType */
   's',0,
@@ -176,6 +190,7 @@ const uint8_t USB_StringDescriptor[] = {
   'n',0,
   'e',0,
   's',0,
+#endif
 /* Index 0x03: Serial Number */
 /* TODO: deploy a real serial number here */
   (12*2 + 2),                        /* bLength (12 Char + Type + lenght) */
